# Repository-level example env file (do NOT add secrets to repo)
# Useful for CI or local reference. Copy values into backend/.env or set in hosting provider.

# Backend
MONGODB_URI=your-mongodb-atlas-connection-string
JWT_SECRET=your-jwt-secret
PORT=10000

# Frontend (Vite)
VITE_API_URL=https://wedding-plan-backend-aws0.onrender.com
VITE_APP_NAME=Wedding Planner
VITE_APP_VERSION=1.0.0
